import React from "react";
import { Text, View } from "react-native";

export default function  Advice({navigation}) {
    return(
        <View>
            <Text>Recomendaciones covicho</Text>
        </View>
    );
}